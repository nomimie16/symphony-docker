
{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <div class="card w-75 p-3 border mx-auto my-5">
        <div class="card-body">
          <h2 class="card-title display-4 titre_article mb-4">Profil Utilisateur</h2>
          <p class="card-text texte"><strong>Email :</strong> {{ app.user.email}}</p>
          <p class="card-text texte "><strong>Mot de passe :</strong> {{ app.user.password}}</p>
          <p class="card-text texte">
            <strong>Role de l'utilisateur :</strong>
            {% for role in user.roles %}
                {{ role }}
             {% endfor %}
            </ul>
        </p>
          <br>
          <a href="#" class="btn gray">Modifier</a>
          <button type="button" class="btn orange" data-bs-toggle="modal" data-bs-target="#deleteModal">
            Supprimer
          </button>
      </div>
    </div>

   

   
{% endblock %}